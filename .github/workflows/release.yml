name: Build and Release

on:
  push:
    tags:
      - "v*" # Trigger on version tags like v1.0.0

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pyqt6 keyboard packaging

      - name: Build with PyInstaller
        run: |
          pyinstaller --noconfirm --onefile --windowed --name "Helldivers2StratCommander" --add-data "assets;assets" --add-data "src/core/stratagem_data.py;." --add-data "src/config/version.py;." --add-data "src/managers/update_checker.py;." --add-data "src/ui/theme_dark_default.qss;src/ui/" --add-data "src/ui/theme_dark_blue.qss;src/ui/" --add-data "src/ui/theme_dark_red.qss;src/ui/" --icon "assets/icon.ico" --manifest "app.manifest" main.py

      - name: Install Inno Setup
        run: |
          choco install innosetup -y

      - name: Create Installer
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
        shell: powershell

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Rename portable executable
        run: |
          $version = "${{ steps.get_version.outputs.VERSION }}"
          Copy-Item "dist\Helldivers2StratCommander.exe" "dist\Helldivers2StratCommander-Portable-v$version.exe"
        shell: powershell

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/installer/Helldivers2StratCommander-Setup-v*.exe
            dist/Helldivers2StratCommander-Portable-v*.exe
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
